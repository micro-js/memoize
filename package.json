{
  "name": "@micro-js/memoize",
  "description": "Create a function that memoizes the result of a &#x60;fn&#x60;.",
  "repository": "git://github.com/micro-js/memoize.git",
  "version": "0.1.1",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
